// tutorial_page_stub.dart - Stub implementation (should not be used directly)

import 'package:flutter/material.dart';

class TutorialPageWeb extends StatelessWidget {
  const TutorialPageWeb({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text('Platform not supported')));
  }
}

class TutorialPageMobile extends StatelessWidget {
  const TutorialPageMobile({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text('Platform not supported')));
  }
}
